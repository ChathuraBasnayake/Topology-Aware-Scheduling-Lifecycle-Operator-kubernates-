apiVersion: v1
kind: Pod
metadata:
  name: test-low-latency
  namespace: default
  annotations:
    topology-aware.io/policy: "low-latency"
    topology-aware.io/target-rack: "rack-a"
spec:
  containers:
    - name: nginx
      image: nginx:alpine
